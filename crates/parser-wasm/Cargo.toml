[package]
name = "parser-wasm"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
parser = { path = "../parser", features = ["schema"] }
wasm-bindgen = "0.2"
schemars = { version = "1", features = ["indexmap2"] }
serde_json = { version = "1" }
